import React from 'react'
import { storiesOf } from '@storybook/react'

storiesOf(
    'Design Guidelines', module
)
    .add(
        'Colors', () => (
            <p>Colors</p>
        )
    )
    .add(
        'Typography', () => (
            <p>Typography</p>
        )
    )
    .add(
        'Icons', () => (
            <p>Icons</p>
        )
    )
    .add(
        'Spacing', () => (
            <p>Spacing</p>
        )
    )
    .add(
        'Scrollbar', () => (
            <p>Scrollbar</p>
        )
    )
    .add(
        'Layout', () => (
            <p>Layout</p>
        )
    )